# Disco Audit — 2026-05-06

**Status:** Inventur abgeschlossen, Phase-2-Quick-Wins werden hier mit Status getrackt.

**Phase-2-Status:**
- F1 ✅ erledigt (2 unused Imports gestrichen)
- F2 ✅ Vermerk: 3 Tools (`extract_pdf_to_markdown`, `pdf_markdown_read`, `project_notes_read`) existieren im Code nicht mehr; Aufruf-Spuren in `chat_messages` bleiben als harmlose Historie. Kein Eingriff.
- F3 (in Arbeit, ohne `start_sync` und ohne `flow_pause` separat — siehe Block C)
- F4 ✅ erledigt (`pdf_classify` entfernt, inkl. `agent/functions/pdf.py`-File und 3 System-Prompt-Stellen)
- F5 ❌ skip (User-Entscheidung 2026-05-06: `report-builder`-Skill bleibt — wird ausgebaut)
- F6 ✅ Vermerk: `markdown-extractor`-Skill ist im Repo nicht mehr da, DB-Spuren harmlos. Kein Eingriff.
- F7 (kommt in Block D)
- F8 (zurückgestellt — gesonderte Diskussion ASCII-vs-Umlaute)
- F9 (kommt nach F1-F8)

**Geltungsbereich:** Code unter `src/disco/`, Skills unter `skills/`, System-Prompt, Migrations, BACKLOG. Keine Inspektion von Kundendaten in `~/Disco/projects/**`.

**Ziel:** ein Befund-Dokument, das die Roadmap für Phasen 2-7 (Quick-Wins, Strukturelles, Bugs, Tests, Doku, Stable-Release) liefert.

---

## A) Code-Karte

**Code-Volumen `src/disco/` — 56 Files, 23 171 LOC, 18 171 SLOC.**

| Modul | Files | LOC | SLOC | Anteil |
|---|---:|---:|---:|---:|
| `agent/` | 19 | 9 335 | **7 466** | 41 % |
| `(root)` | 10 | 3 659 | 2 780 | 15 % |
| `flows/` | 7 | 3 220 | 2 482 | 14 % |
| `api/` | 3 | 2 384 | 1 889 | 10 % |
| `chat/` | 3 | 1 380 | 1 136 | 6 % |
| `docs/` | 7 | 1 446 | 1 100 | 6 % |
| `sharepoint/` | 5 | 1 428 | 1 085 | 6 % |
| `pdf/` | 2 | 319 | 233 | 1 % |

**Top-10 Files nach SLOC:**

| SLOC | LOC | Datei |
|---:|---:|---|
| 1 774 | 2 233 | `api/main.py` |
| 1 080 | 1 429 | `flows/sdk.py` |
| 1 031 | 1 387 | `agent/core.py` |
| 906 | 1 075 | `agent/functions/sources.py` |
| 859 | 1 028 | `chat/repo.py` |
| 829 | 1 007 | `cli.py` |
| 741 | 993 | `flow_notifications.py` |
| 710 | 912 | `agent/functions/flows.py` |
| 683 | 867 | `agent/functions/search.py` |
| 680 | 859 | `agent/functions/fs.py` |

**Andere Bereiche:**

| Bereich | Files | Lines |
|---|---:|---:|
| `skills/` (Markdown) | 11 | 2 814 |
| `migrations/` (SQL) | 25 | 1 492 |
| `docs/` (Markdown) | 6 | 4 212 |
| `system_prompt.md` allein | 1 | 782 (~38 KB, 41 Sektionen) |

**Erste Beobachtungen:**
- `api/main.py` mit 1 774 SLOC ist die größte Single-File und vereinigt REST + WebSocket + statisches Frontend-Routing — Splitting-Kandidat.
- `flows/sdk.py` 1 080 SLOC: das ist die FlowRun/FlowDB-Abstraktion. Groß, aber inhaltlich eine logische Einheit. Eher Refactor in Sub-Files als Inhalt streichen.
- `agent/core.py` 1 031 SLOC: AgentService + Stream-Logik + Foundry-Calls. Ähnlich, große Einheit.
- `agent/`-Modul stellt mit 41 % den Hauptbrocken — das ist die Disco-Persönlichkeit. Inkl. 49 Custom Functions in `agent/functions/`.
- `docs/` (Doc-Engines: pdf, image, dwg, excel) ist mit 1 100 SLOC ungewöhnlich groß für 7 Files — Engine-Logik tief.
- `pdf/` mit 233 SLOC ist klein und steht neben `docs/` (das auch PDFs macht). **Doppelung verdächtig**, zu prüfen.

## B) Tot-Code-Detektion

`uvx vulture src/disco/` mit `--min-confidence 80`:

| Datei:Zeile | Befund | Confidence |
|---|---|---:|
| `cli.py:11` | unused import `projects_module` | 90 % |
| `sharepoint/graph.py:15` | unused import `quote` | 90 % |

**Ergebnis: Codebase ist sehr sauber.** Nur zwei nicht genutzte Imports — beide trivial entfernbar.

**60%-Confidence-Lauf** liefert weitere ~50 Treffer, die aber **alle False-Positives** sind:
- Tool-Functions mit `@register(name="...")`-Decorator (z.B. `_fs_list`, `_xlsx_inspect`, `_flow_*`) — Vulture sieht keinen direkten Python-Aufruf, weil sie über den Tool-Dispatcher (Foundry → Tool-Schema → Function) laufen.
- `use_project` (Context-Manager) — dynamisch via `with use_project(...)` benutzt, aber Vulture sieht's nicht.
- openpyxl-Style-Attribute (`fill`, `font`, `alignment`, …) — werden auf Cell-Objekten gesetzt, Vulture missdeutet das als toter Setter.

→ **Keine echten Streichkandidaten** über die zwei klaren Imports hinaus. Das ist eine sehr beruhigende Botschaft.

## C) Tool-Use-Statistik

Quelle: `chat_messages.tool_calls_json` über die letzten **30 Tage** (Prod `system.db`). 49 Tools registriert, **44** wurden tatsächlich gerufen, **7** sind in 30 Tagen ungenutzt geblieben. Plus 3 Aufrufe in der DB für Tools, die im aktuellen Code **nicht mehr existieren** (Legacy-Calls aus historischen Sessions).

### Top-Tools (täglich gebraucht)

| Tool | Calls/30d |
|---|---:|
| `sqlite_query` | 1398 |
| `memory_read` | 263 |
| `run_python` | 232 |
| `sqlite_write` | 188 |
| `fs_read` | 186 |
| `flow_status` | 186 |
| `fs_list` | 139 |
| `load_skill` | 123 |
| `fs_write` | 113 |
| `flow_run` | 112 |

### Mittel-genutzte Tools (10–110 Aufrufe)

`fs_search`, `memory_append`, `list_skills`, `search_index`, `flow_show`, `xlsx_inspect`, `doc_markdown_read`, `flow_runs`, `plan_list`, `plan_write`, `sources_register`, `flow_items`, `plan_append_note`, `build_xlsx_from_tables`, `search_documents`, `flow_create`, `sources_attach_metadata`, `flow_logs`, `flow_list`, `import_xlsx_to_table`, `plan_read`, `fs_delete`, `flow_cancel`.

### Selten — prüfen

| Tool | Calls/30d | Bewertung |
|---|---:|---|
| `memory_write` | 9 | OK — explizite Memory-Stelle, soll selten sein |
| `pdf_markdown_read` | 7 | Aufruf-Tool ohne aktuellen Code-Pendant — DB-Legacy |
| `import_csv_to_table` | 6 | Selten, aber sinnvoll wenn doch mal CSV |
| `fs_mkdir` | 6 | OK |
| `build_search_index` | 5 | Sollte selten sein, ist OK |
| `sources_detect_duplicates` | 4 | OK |
| `flow_fork` | 4 | Spezial-Use-Case, OK |
| `extract_pdf_to_markdown` | 4 | Code existiert nicht mehr — Legacy |
| `pdf_classify` | 3 | seit Routing-Flow obsolet — **Streichkandidat** |
| `project_notes_read` | 1 | Code existiert nicht mehr — Legacy |
| `get_project_details` | 1 | Sehr selten — prüfen |
| `get_database_stats` | 1 | Sehr selten — prüfen |

### Registriert aber **NIE in 30 Tagen gerufen** — Streichkandidaten

| Tool | Vermutung |
|---|---|
| `extract_markdown_structure` | unklarer Use-Case |
| `flow_pause` | Disco pausiert nicht selbst |
| `fs_read_bytes` | binäres Lesen wird nicht gebraucht |
| `fs_write_bytes` | binäres Schreiben wird nicht gebraucht |
| `list_documents` | obsolet, durch `search_index` ersetzt |
| `list_projects` | Disco kennt das Projekt aus Context |
| `start_sync` | SharePoint-Sync, manuell auslösbar via UI |

→ **7 Tools entfernbar** plus 3 Code-Legacy-Aufrufe (existieren nicht mehr im Code).

## D) Skill-Use-Statistik

11 Skills im Repository (`skills/*.md`), alle wurden mindestens einmal in 30 Tagen geladen. Plus 1 Aufruf für `markdown-extractor`, der nicht mehr im Repo liegt (Legacy).

| Skill | Loads/30d | Bewertung |
|---|---:|---|
| `flow-supervisor` | 33 | Top-Skill (Flow-Monitoring) |
| `project-onboarding` | 18 | Pflicht bei jedem neuen Thread |
| `planning` | 15 | Sinnvoll |
| `sources-onboarding` | 13 | OK |
| `sdk-reference` | 9 | Flow-Builder-Companion |
| `flow-builder` | 9 | OK |
| `context-onboarding` | 9 | OK |
| `excel-formatter` | 6 | OK |
| `excel-reporter` | 5 | OK |
| `python-executor` | 4 | OK |
| `report-builder` | **1** | **Selten — Streichen oder schärfer triggern?** |

→ `report-builder` ist der einzige mit fragwürdiger Nutzungsfrequenz. Inhalt prüfen — vielleicht in `excel-reporter` integrieren, statt eigenständig?

## E) BACKLOG-Audit

**Größe:** 3 268 Zeilen, 114 `###`-Sub-Einträge, 24 `##`-Top-Sections.

### Strukturelle Befunde

1. **Aufgeblähte Hoch-Priorisierung.** 24 Einträge mit `Prioritaet: hoch` + 11 weitere mit `Priorität: hoch` (Schreibweise gemischt) — wenn 35 von 114 als „hoch" markiert sind, hat die Priorisierung ihre Funktion verloren. Faustregel: **maximal 5–7 echte Top-Einträge**, alles andere fällt eine Stufe runter.

2. **Inkonsistente Umlaute.** „Prioritaet" (ASCII) vs „Priorität" (Umlaut) wechseln sich ab. ASCII-only-Konvention müsste durchgezogen werden (so wie wir's im Code haben).

3. **Phantom-DONE-Risiko.** Nur 6 explizite DONE-Marker bei 114 Einträgen, aber wir haben in den letzten Wochen viel umgesetzt — viele Einträge sind real erledigt, aber nicht markiert. Ein systematisches Diff zwischen BACKLOG und Commits der letzten 30 Tage liefert die Kandidaten.

4. **★-Konsolidierungs-Eintrag „EXTRACTION-PIPELINE OVERHAUL"** (Zeile 3122) fasst 7 ältere Einträge zusammen, die aber **alle noch im BACKLOG stehen** (als „Vertiefung"). Ergebnis: derselbe Punkt wird teils 2× gefunden. Empfehlung: alte Einträge in den ★-Eintrag inline einarbeiten, dann oben löschen.

5. **8 explizite „(spaeter)"-Tags** — als bewusste Rückstellung ok, sollten aber in einer eigenen Section „Später / strategisch" gesammelt werden, statt durch das ganze Dokument verteilt zu sein.

### Top-Sections (alle 24)

```
9     UI / Chat-Erlebnis
117   Report-Format / Analyse-Ergebnisse
170   Chat-Funktionalität
210   Modell-Einstellungen
258   Projekt-Template / Wissens-Dateien
351   UI / Layout                         ← redundant mit "UI / Chat-Erlebnis"?
365   Document Intelligence
486   Sicherheit / Projekt-Isolation
566   Flows / Worker-System
783   Docling / MLX
798   Architektur
903   Release / DevOps
927   Technische Schuld + Setup-Probleme
1004  Pipeline-Vollstaendigkeits-Sicht    ← Teil vom ★-Konsolidat
1049  Flow-UI im Chat-Fenster (DONE)
1080  Office-Formate in die Extraction    ← Teil vom ★-Konsolidat
1118  Extraction nur auf kanonische       ← Teil vom ★-Konsolidat
1186  Public-Workspace fuer Cross-Projekt
1253  Run-Strip Bugs
1297  Cost-Tracking fuer GPT-5.1-Vision   ← teilweise erledigt 2026-05-06
1354  Anhaltspunkte fuer replaces/...     ← Teil vom ★-Konsolidat
1452  Relevance-Score / Document-Scoring
1494  OpenAI Evals / Foundry Evaluations
1566  Stabilitaets-Bugs aus FTS5-Deadlock
1695  Disco-Prozess-Management
1790  Sidebar-Navigation skaliert nicht
1874  Extraction-Pipeline-UX              ← Teil vom ★-Konsolidat
2073  File-Internal-Metadata              ← Teil vom ★-Konsolidat
2210  Cost-Tracking: Chat + Monatlich
2371  Data-Lineage / Tracing
2530  User-Feedback-Cluster aus 24 bad
2808  Datenverarbeitung auf Ebene 3
3020  Klickbare Links + Thumbnails       ← teilweise erledigt 2026-05-04
3122  ★ EXTRACTION-PIPELINE OVERHAUL     ← konsolidiert; teilweise erledigt
```

### Konkrete Streichungs-Kandidaten (Verdacht)

- **Zeile 1049 „Flow-UI im Chat-Fenster (DONE)"** — kann komplett gelöscht (oder in CHANGELOG verschoben).
- **Zeile 1004 / 1080 / 1118 / 1354 / 1874 / 2073** — alles Bestandteil des ★-Konsolidats (Zeile 3122). Bei der Gelegenheit prüfen, was schon umgesetzt ist und entsprechend abhaken.
- **Zeile 351 „UI / Layout"** vs Zeile 9 „UI / Chat-Erlebnis" — vermutlich zu einer Section mergebar.
- **Zeile 1297 Cost-Tracking GPT-5.1-Vision** — der Kern-Bug ist seit 2026-05-06 (heute) gefixt; Eintrag entsprechend kürzen.

→ Erwarteter Aufräum-Effekt: BACKLOG.md von 3 268 Zeilen auf grob **1 800–2 000 Zeilen**, von 114 Sub-Einträgen auf grob **70–80**. Das ist Sache von Phase 2 (Quick-Wins-Sessions).

## F) Migrations-Audit

**Migrations-Files:**
- System-DB (`migrations/`): 9 Files (001–009), aktuell auf v9.
- Projekt-DB datastore (`migrations/project/datastore/`): 9 Files.
- Projekt-DB workspace (`migrations/project/workspace/`): 5 Files.

**Tatsächliche Tabellen pro DB (Beispiel `ampel-test`):**

| DB | Tabellen | davon FTS-intern |
|---|---:|---:|
| `system.db` | 13 | – |
| `datastore.db` | 17 | 5 (`agent_search_chunks_fts*`) |
| `workspace.db` | 8 | – |

### Verdacht-Tabellen für Bereinigung

#### 1. `agent_pdf_inventory` — überholtes Konzept

Heute beim Routing-Bug-Fix wurde klar: `work_extraction_routing.file_id` und `agent_doc_markdown.file_id` verweisen in Wirklichkeit auf `agent_sources.id`, **nicht** auf `agent_pdf_inventory.id` (Schema-Kommentar war veraltet). `agent_pdf_inventory` enthält nur PDFs und ist seit dem Multi-Format-Routing kein vollständiges Inventar mehr.

**Referenziert in 7 Files:**
- `agent/system_prompt.md`
- `agent/functions/sources.py` (sources_register synct dorthin)
- `agent/functions/data.py`
- `api/main.py`
- `flows/library/__init__.py`
- `flows/sdk.py`
- `flows/README.md`

**Empfehlung:** in Phase 3 (Strukturelles) per 3-Schritt-Migration entfernen — Code auf `agent_sources` umstellen, später Tabelle droppen. Spart Sync-Aufwand bei jedem `sources_register`-Lauf, eliminiert konzeptionelle Doppelung.

#### 2. SharePoint-Connector-Tabellen — vermutlich inaktiv

Aus Tool-Use-Statistik: `start_sync` wurde in 30 Tagen **nie** gerufen. Damit sind die zugehörigen Tabellen Verdacht für „inaktive Phase-1-Reste":
- `documents` (Refs: 11 Files — vermutlich auch in alten CLI-Pfaden)
- `source_folders` (Refs: 5 Files)
- `processing_events` (Refs: 1 File — sehr wenig)
- `document_sp_fields` (Refs: 4 Files)

**Architektur-Frage an Dich:** ist der SharePoint-Connector strategisch noch geplant (Phase 1 hieß ja mal „Document Library aus SP synchronisieren"), oder hat sich das mit der heutigen `sources_register`-+-rsync-Praxis erledigt? Falls obsolet → größerer Streichungs-Block: 4 Tabellen + ~5 Files Code (`disco/sharepoint/`, 1 085 SLOC).

#### 3. FTS-Search-Tabellen sind OK

5 Tabellen (`agent_search_chunks_fts*`) sind alle interne SQLite-FTS5-Indexstrukturen — gehören zur sichtbaren `agent_search_chunks_fts`-Virtual-Table. Nicht löschbar einzeln, nur als Gesamt-Index.

### Migrations-Hygiene

- Alle Migrations-Files mit Datums-Header versehen? **Nein.** Sollte in Phase 6 (Doku) als Konvention etabliert werden.
- README in beiden Projekt-DB-Migrations-Verzeichnissen vorhanden — gut.
- Keine *.sql.bak / *.sql.old o.ä. Gerümpel — gut.

## G) System-Prompt-Audit

**Größe:** 38 KB, 782 Zeilen, **41 Sections** (12 Top-Level `##`, 29 Sub-Level `###`).

### Top-10 längste Sections (Anteil am Volumen)

| Zeilen | Section | Bewertung |
|---:|---|---|
| 57 | `### Extraction-Pipeline — Registrieren → Routing → Extraktion → Lesen` | Inhaltlich dicht, aber wiederholt teils, was Skills/Tool-Descriptions sagen |
| 54 | `## Wer Du bist` | Persönlichkeits-Definition. Heute eher lang — könnte gestrafft werden |
| 53 | `### Architektur-Ebenen — wo liegt was?` | Nützlich, aber teilweise Doppelung mit „Verzeichnisstruktur" |
| 46 | `### Die harten Regeln` | Pflicht-Block — eher behalten |
| 43 | `### Volltext-Suche im Projekt (FTS5)` | Sehr detailliert — könnte gekürzt werden, weil Trigger-Tabelle (oben) das Routing schon macht |
| 42 | `## Skill-System: bei diesen Triggern Skill laden` | Trigger-Tabelle. Mit dem Streichen toter Skills (siehe D) wird das automatisch kleiner |
| 34 | `### Verzeichnisstruktur` | Redundant zu `### Architektur-Ebenen` — mergebar |
| 34 | `### Excel — zwei Modi` | Heute aktualisiert — ist OK |
| 33 | `### Live-Kommentar — vor jedem Tool-Call eine Zeile` | Lange Erklärung für eine Verhaltensregel — könnte halbiert werden |

### Kürzungs-Kandidaten

1. **Tool-Detail-Sektionen** (`### Dateisystem`, `### Datenbank`, `### Quellen-Verwaltung`, `### Excel — zwei Modi`, `### Lokale Python-Ausfuehrung`, …): die Tool-Descriptions stehen schon im Foundry-Schema. Hier muss nur die **Routing-Logik** stehen („wann nimmst du was"), nicht die Parameter-Liste pro Tool. Schätzung: 50–60 Zeilen einsparbar.

2. **Doppelung Verzeichnisstruktur ↔ Architektur-Ebenen**: 34 + 53 = 87 Zeilen, die teils dasselbe sagen. Konsolidiert auf eine zusammenhängende Section ~50 Zeilen.

3. **„Wer Du bist" + „Arbeitsstil" + „Grenzen"** (Zeilen 3, 726, 746): drei separate Persönlichkeits-Sektionen — könnten zu einer kompakten 30-Zeilen-Section verschmelzen.

4. **Anti-Halluzination + Faktenbasiert + kein Raten**: drei eng-verwandte Blöcke (Zeilen 341–374) — eine konsolidierte Regel mit Beispielen reicht.

5. **„Pipeline-Durchlauf nach Source-Onboarding" (Zeile 276) + „Extraction-Pipeline …" (Zeile 555)**: doppelter Pipeline-Hinweis. Mergen.

### Geschätzter Effekt nach Phase 3 (Strukturelles)

Realistisch: **782 Zeilen → 540–580 Zeilen** (-25–30 %). Bei jeder Streichung Trigger-Tests fahren, dass Disco bei den bekannten Use-Cases noch korrekt routed.

### Was nicht angetastet werden sollte

- Skill-System-Trigger-Tabelle (Zeile 435 ff.) — das ist Discos zentrale Routing-Logik
- „Die harten Regeln" (Zeile 195) — Pflicht-Verhalten
- Beispiel-Dialog am Ende (Zeile 759) — wirksamer Anker für Format-Erwartungen

## H) Layer-Check

### Architekturelle Layer-Verstöße — **keine** ✅

- `agent/` ↛ `api/` — keine Aufwärts-Imports
- `flows/` ↛ `api/` — keine Aufwärts-Imports
- `api/` ↛ `agent/functions/` — keine Internals-Zugriffe (api nutzt nur den public AgentService)
- `agent/functions/*` ↛ `agent/functions/*` — keine gegenseitigen Imports (Tool-Functions sind unabhängige Einheiten)

Das ist eine **sehr saubere Schichten-Architektur** — überraschend gut für 23k SLOC und mehrere Personen-Monate Entwicklung.

### Beobachtung — `sqlite3`-Direkt-Imports breit verteilt

11 Stellen importieren `sqlite3` direkt:

```
src/disco/sources.py
src/disco/flow_notifications.py
src/disco/projects.py
src/disco/agent/functions/sources.py
src/disco/agent/functions/imports.py
src/disco/agent/functions/doc_markdown.py
src/disco/agent/functions/search.py  (zweimal)
src/disco/agent/functions/data.py  (zweimal)
```

Das ist **kein Layer-Verstoß**, aber jede Stelle setzt eigene Connection-Optionen (WAL-Modus, busy_timeout, foreign_keys, row_factory). Drift-Potenzial: wenn zukünftige Migrations einen Default ändern, bleibt's an manchen Stellen alt. Konsolidierungs-Vorschlag (Phase 3): einen zentralen `connect()`-Helper unter `disco.db_helpers` anbieten und alle Stellen darauf umstellen.

Nicht kritisch, aber niedrig-hängende Frucht.

---

## Findings (Roadmap)

Sortiert nach **Aufwand×Risiko** — niedriger Aufwand + niedriges Risiko zuerst.

### Quick-Wins (niedriges Risiko, geringer Aufwand) — Phase 2

| ID | Befund | Aufwand | Aktion |
|---|---|---|---|
| F1 | 2 unused Imports (`projects_module` in cli.py, `quote` in graph.py) | XS | löschen |
| F2 | 3 Tool-Aufrufe in DB für nicht mehr existierende Tools (`extract_pdf_to_markdown`, `pdf_markdown_read`, `project_notes_read`) | XS | nichts tun (DB-Legacy, harmlos), evtl. CLAUDE.md-Hinweis |
| F3 | 7 Tools registriert aber 0× in 30 Tagen gerufen (`extract_markdown_structure`, `flow_pause`, `fs_read_bytes`, `fs_write_bytes`, `list_documents`, `list_projects`, `start_sync`) | S | streichen aus `agent/functions/`, `disco agent setup` für beide Agents |
| F4 | `pdf_classify` nur 3× gerufen, durch `extraction_routing_decision`-Flow obsolet | XS | streichen |
| F5 | `report-builder`-Skill nur 1× geladen | XS | inhaltlich prüfen, ggf. in `excel-reporter` mergen oder streichen |
| F6 | `markdown-extractor`-Skill in DB-Calls aber nicht im Repo | XS | aufräumen / Hinweis |
| F7 | BACKLOG.md: 6+ DONE-Einträge fehlen Markierung; ~7 Einträge gehören zu ★-Konsolidat aber stehen noch separat | S | systematisches Aufräumen, Sektionen mergen |
| F8 | BACKLOG.md: „Prioritaet" vs „Priorität" gemischt | XS | sed-Replace auf ASCII-Schreibweise |
| F9 | BACKLOG.md: 35 von 114 Einträgen als „hoch" — Priorisierung erodiert | M | Re-Priorisierung; max 7 echte Top-Einträge |

### Strukturelles (mittleres Risiko, mittlerer Aufwand) — Phase 3

| ID | Befund | Aufwand | Aktion |
|---|---|---|---|
| F10 | System-Prompt 38 KB / 782 Zeilen / 41 Sections — Redundanzen + Tool-Detail-Wiederholung | M | auf ~540-580 Zeilen straffen; Sections mergen, Tool-Details raus |
| F11 | Skill-System-Tabelle wird kleiner mit F3 + F5 — wartet auf F-Streichungen | S | als Folge-Aktion nach F3+F5 |
| F12 | `agent_pdf_inventory` ist konzeptionell überholt (file_id verweist auf agent_sources, nicht inventory) | M | 3-Schritt-Migration: Code umstellen → Tabelle droppen, danach `sources_register` einfacher |
| F13 | 11 Stellen mit eigenem `import sqlite3` + Connection-Setup — Drift-Risiko bei Migration | M | zentralen `disco.db_helpers.connect()` anbieten, alle Stellen umstellen |
| F14 | „UI / Layout" und „UI / Chat-Erlebnis" sind zwei BACKLOG-Sections — vermutlich mergebar | S | BACKLOG-Strukturierung |

### Architektur-Entscheidung — eigene Diskussion

| ID | Befund | Aufwand | Aktion |
|---|---|---|---|
| F15 | **SharePoint-Connector** vermutlich inaktiv (Tool `start_sync` 0× in 30d). 4 Tabellen + 1 085 SLOC unter `disco/sharepoint/` | L | **Architektur-Entscheidung User**: strategisch behalten oder komplett streichen? Falls letzteres: 3-Schritt-Migration (Tabellen leeren, Code raus, Tabellen droppen) |
| F16 | `pdf/` (233 SLOC) parallel zu `docs/pdf.py` — Doppel-Logik? | S | im Detail prüfen, vermutlich konsolidierbar |

### Restbugs (separat) — Phase 4

| ID | Befund | Aufwand |
|---|---|---|
| F17 | Pipeline-Status Phase 6 Phase B: `error`-Spalte in `work_extraction_routing` + `agent_doc_markdown` für 🟡-Status | M |
| F18 | Run-Strip Bug 2: Counter springt nicht auf 100 % bei done+1failed | XS |
| F19 | Counter-Konsistenz nach Crash (workspace.db WAL-Recovery) | M |

### Tests + CI/CD — Phasen 5+8

| ID | Befund | Aufwand |
|---|---|---|
| F20 | Keine Tests vorhanden für Pipeline-Status-Endpoint, Cost-Tracking, Migration-Smoke. Heutiger Schema-Bug + Cost-Bug wäre durch Tests gefangen worden | L |
| F21 | Test-Daten-Korpus fehlt (Phase 8.3) | M |
| F22 | E2E-Test-Framework Browser-MCP fehlt (Phase 8.2) | L |

### Dokumentation — Phase 6

| ID | Befund | Aufwand |
|---|---|---|
| F23 | CLAUDE.md vs heutiger Code: einige Konventionen sind neu (kein Auto-Deploy, Pricing-Verifikation, Backup-Routine) | S |
| F24 | Skill-Frontmatter-Inkonsistenzen — manche Skills haben `when_to_use`, andere nicht | S |
| F25 | Migrations-Files ohne Datums-Header | XS |

---

## Kennzahlen-Zusammenfassung (Stand 2026-05-06)

- **23 171 LOC** in 56 Python-Files unter `src/disco/`
- **49 Tools** registriert, davon **44 aktiv**, **7 dead**, 3 Code-Legacy
- **11 Skills** im Repo, alle aktiv, einer (`report-builder`) mit fragwürdiger Frequenz
- **38 KB System-Prompt** mit 41 Sections — Straffungs-Potenzial 25–30 %
- **9 + 9 + 5 = 23 Migrations**, mind. 1 Tabelle (`agent_pdf_inventory`) konzeptionell überholt
- **3 268 Zeilen BACKLOG**, ~50–60 % wahrscheinlich redundant oder erledigt
- **Layer-Architektur sauber** — keine Verstöße zwischen den Modulen

**Reifegrad-Eindruck:** sehr hoch für ein Projekt dieser Größe. Wenig totes Bisschen, klare Schichten, das Aufräumen ist eher Hygiene als Strukturwandel.

**Realistische Aufwand-Schätzung Phasen 2–7:**
- Phase 2 Quick-Wins: 2–3 kleine Sessions
- Phase 3 Strukturelles: 4–6 mittlere Sessions (System-Prompt + agent_pdf_inventory-Migration sind die größten Brocken)
- Phase 4 Restbugs: 2 Sessions
- Phase 5 Tests: 3–4 Sessions (Test-Daten-Korpus + Unit-Tests)
- Phase 6 Doku: 1–2 Sessions
- Phase 7 Stable-Release: 1 Session
- Phase 8 Test-Automatisierung + CI/CD + Daten-Trennung: 3–5 Sessions

Insgesamt: **15–22 Sessions** bis zum Stable-1.0.
